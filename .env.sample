TAG=latest

ATLAS_CONNECTION=mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority
BACKEND_LOCAL_PORT=8081
BACKEND_DOCKER_PORT=8081 # setting this does not change docker port mapping, this is just for convenience
CLIENT_ORIGIN=http://localhost:8080

REACT_LOCAL_PORT=8080
REACT_DOCKER_PORT=8080 # setting this does not change docker port mapping, this is just for convenience
REACT_APP_BACKEND_URL=http://localhost:8081